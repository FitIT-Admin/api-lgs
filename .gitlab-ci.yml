build-Project:                                 
 stage: build                                  
 image: trion/ng-cli                           
 before_script:
  - echo "Iniciando la compilación del código"
  #- cd api-nielsen    
 script:
  - echo "$PRIVATE_KEY_GUSSS" > gusss.key     
  - chmod 600 gusss.key                   
  - ls -lah                                  
  - ssh-agent bash -c -v 'ssh-add gusss.key; scp -o StrictHostKeyChecking=no -r *  gusss@159.65.163.63:/home/gusss/api-nielsen-dev/'
